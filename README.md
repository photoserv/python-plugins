# python-plugins

Python plugins for Photoserv

Browse `plugins/` and pick some plugins you want to run.
Install them in Photoserv by placing them in `/plugins` (docker) or `./plugins` (local dev).

## Contributing

Make sure to **generate a UUID4** and submit a `<plugin>.md` documentation file alongside the Python file.
